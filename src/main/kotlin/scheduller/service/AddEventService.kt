package scheduller.service

import scheduller.data.Event
import scheduller.data.Scheduler
import scheduller.data.User

class AddEventService {

    fun addEvent(event: Event, user: User, scheduler: Scheduler) = {

    }
}
